"use client";
import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#475b3a",
      contrastText: "#FFFFFF",
    },
    secondary: {
      main: "#475b3a",
      contrastText: "#FFFFFF",
    },
  },
});
export default theme;
